import 'package:flutter/material.dart';

class MakeReservationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Faça uma reserva"),
      ),
      body: Center(
        child: Text("Make a Reservation Screen"),
      ),
    );
  }
}
